<h1>convert jquery to javascript</h1>

<h2>how it works:</h3>

1.) app.init();

2.) app.preload();

3.) app.getDeclaredVar();

4.) app.convert(); // find all $(...)...;
	a.) convert all declaredVar
	b.) get in $(...)...(); and convert all.